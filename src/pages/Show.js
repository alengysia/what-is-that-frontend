import { useState } from "react";
import {Link} from "react-router-dom"

function Show(props) {
    return (
        <div>
            <h1>This is the Show page</h1>
        </div>
    );
}

export default Show;